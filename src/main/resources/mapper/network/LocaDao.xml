<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hq.modules.network.dao.LocaDao">

    <insert id="insertLocations" keyProperty="location_id">
        insert into net_location (location_id, location_name, netOffset, convBoundary,origBoundary) values (#{location_id},#{location_name},#{netOffset},#{convBoundary},#{origBoundary})
    </insert>

</mapper>